// import { create } from 'zustand';

// type StoreBase = {
//   sidebarClose: boolean;
//   setSidebarCloseState: (status: boolean) => void;
// };

// export const useStoreBase = create<StoreBase>((set) => ({
//   sidebarClose: false,
//   setSidebarCloseState: (state: boolean) => set({ sidebarClose: state }),
// }));
